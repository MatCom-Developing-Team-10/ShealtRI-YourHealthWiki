# Copy this file to .env and fill in the values.
# docker-compose reads .env automatically (env_file directive).

# Groq API key for profile-aware RAG generation.
# Without it the app still runs and falls back to a template response,
# but the answers will be much less useful.
# Get one for free at https://console.groq.com/keys
GROQ_API_KEY=
