# Runtime dependencies — only what the FastAPI app actually loads.
# ============================================================
# Anything used only by offline scripts (PDF ingestion, semantic
# chunking experiments, the crawler) is intentionally NOT listed
# here so the Docker image stays small and the first build is fast.

# Numerics
numpy>=1.26.0
scipy>=1.13.0

# Machine Learning & NLP
scikit-learn>=1.5.0          # TF-IDF, TruncatedSVD (LSI model)
nltk>=3.8.0                  # Stopwords, tokenization utilities
spacy>=3.7.0                 # Lemmatisation, POS tagging
# After install: python -m spacy download es_core_news_md

# Model persistence
joblib>=1.4.0

# Vector database
chromadb>=0.5.0

# RAG & environment
python-dotenv>=1.0.0
groq>=0.11.0                 # Groq API for profile-aware RAG

# Web search fallback (DuckDuckGo + page fetch/parse)
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
ddgs>=0.1.0

# Utilities
python-dateutil>=2.8.0
pydantic>=2.0.0

# Ranking
rank-bm25>=0.2.2             # BM25 scoring for hybrid re-ranking
